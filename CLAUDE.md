# 미디어커머스 업계 동향 모니터링 (작업 지시문)

## 목적
자사 4개 브랜드가 속한 업계의 동향·특이사항을 주기적으로 크롤링·정리한다.
결과물은 HTML 리포트로 만든다.

## 자사 브랜드 & 모니터링 버티컬
- 맘앤트윈스 — 화장품/뷰티 (스킨케어·색조)
- 리아네이처 — 화장품/뷰티 (스킨케어·색조)
- 리끌로우 — 아이웨어(선글라스·안경), 레인부츠·패션잡화
- 더리아 — 뷰티 디바이스(홈뷰티 기기)

## 추적 카테고리 (이 4개로 섹션을 나눈다)
1. 화장품/뷰티
2. 아이웨어(선글라스·안경) + 레인부츠·패션잡화
3. 뷰티 디바이스(홈뷰티 기기)
4. 미디어커머스 공통 — 라이브커머스, 커머스 플랫폼(쿠팡·네이버·올리브영 등) 정책, 인플루언서/퍼포먼스 마케팅 트렌드

## 수집 항목 (각 카테고리에서)
- 신제품·신규 브랜드 런칭
- 경쟁사 마케팅/캠페인 사례
- 라이브커머스·플랫폼 동향 및 정책 변화
- 규제·인증·이슈 (화장품법, KC인증, 의료기기 광고 규정 등)
- 소비 트렌드·시장 데이터

## 매체 티어 — 금지 목록이 아니라 '채우는 순서'
티어는 **우선순위**다. 상위 티어부터 채우고, 카드가 모자라면 아래 티어로 내려간다.
다만 **어느 티어에서 가져오든 아래 '광고성 배제 기준'은 예외 없이 적용**한다. 티어가 낮다고 버리는 게 아니라,
광고성이면 버린다. 같은 사안을 여러 매체가 다뤘으면 **항상 상위 티어 기사를 링크**한다.

**티어 1 — 여기서 먼저 채운다**
전자신문, 서울경제, 매일경제, 한국경제, 이투데이, 대한경제, 시사저널e, 디지털데일리,
바이라인네트워크, 플래텀, 아웃스탠딩, 모비인사이드, 어패럴뉴스, 패션비즈, 한국섬유신문
해외(선택): Modern Retail, Glossy, Digiday, Business of Fashion

**티어 2 — 업종 전문지. 티어1에 없는 이슈를 메운다**
코스인코리아, 코스모닝, 장업신문, 뷰티경제(thebk), 뷰티누리(화장품신문)
→ 원료·규제·시장 데이터는 오히려 여기가 빠르고 깊다. 적극 쓴다.
   단일 브랜드 신제품 출시·판매 실적 홍보성 기사는 광고성 기준으로 거른다.

**티어 3 — 위에서 못 채웠을 때만. 대신 기준을 더 깐깐하게**
보도자료 배포처 성격·신생 매체(중앙이코노미뉴스, AI마케팅뉴스, 신아일보, 천지일보, 플라넷뉴스 등),
협찬·화보성 매거진(엘르, 코스모폴리탄 등).
→ 그 이슈를 티어1·2가 아무도 안 다뤘고, **기사 내용 자체가 시장·정책·트렌드 정보**일 때만 채택한다.
   본문 사실관계를 다른 출처로 한 번 더 확인한 뒤 카드로 만든다.
→ 블로그·브랜드 자사 뉴스룸·쇼핑몰 페이지는 티어에 들어가지 않는다. 기사가 아니라 홍보물이므로 쓰지 않는다.

## 광고성 배제 기준 (티어 무관, 하나라도 걸리면 버린다)
- 보도자료를 거의 그대로 실은 기사 (기자 해석·비교·업계 코멘트가 없다)
- 특정 브랜드 하나의 출시·할인·판매량 자랑이 본문의 중심
- 수치의 출처가 그 브랜드 자체 발표뿐이고 제3자 데이터가 없다
- 본문에 구매 링크·판매 채널·가격·프로모션 기간이 안내돼 있다
- 제목이 '대박', '폭증', '완판', '1위 등극' 같은 홍보 문구다

## 카드가 모자랄 때 (기간은 절대 늘리지 않는다)
순서대로 시도한다. **마지막까지 안 되면 그 섹션 카드 수가 적은 채로 낸다. 옛날 기사로 메우지 않는다.**
1. 티어1 안에서 검색어를 바꿔 다시 찾는다 (브랜드명·제도명·수치 키워드)
2. 티어2로 내려간다
3. 인접 주제로 넓힌다 (예: 아이웨어가 없으면 패션잡화·디자인 IP·소재 트렌드)
4. 해외 매체를 본다
5. 티어3에서 위 조건(티어1·2 미보도 + 정보성 + 교차확인)을 만족하는 기사를 찾는다

## 수집 창 — 전 섹션 '지난 7일' 엄수
주간 리포트다. **네 섹션 모두 지난 7일에 발행된 기사만 쓴다.** 창 밖 기사는 내용이 좋아도 채택하지 않는다.
빈자리를 옛날 기사로 조용히 메우지 않는다. 카드가 모자라면 검색 소스를 넓히지, 기간을 늘리지 않는다.

## 작업 절차
1. 위 카테고리별로 지난 7일 기사·소식을 수집한다.
   검색보다 **`기사수집.py`(매체 목록 직접 스캔)를 먼저 돌리고**, 부족한 카테고리만 웹 검색으로 보완한다.
   ```
   python 기사수집.py 2026-06-29 2026-07-05      # 기간 내 발행 기사만 TSV로 출력
   ```
2. **발행일 검증(필수).** 후보 기사마다 원문 HTML에서 발행일을 뽑아 지난 7일 안에 있는지 확인한다.
   창 밖이면 카드로 쓰지 않는다. 검색 결과의 노출 순서·관련성은 최신성을 보장하지 않는다.
   ```
   curl -sL -A "Mozilla/5.0" "URL" | grep -ioE '<meta[^>]+(article:published_time|datePublished)[^>]*>'
   ```
   (메타태그가 없으면 본문 상단 기사입력일을 확인하고, 그래도 없으면 그 기사는 버린다.)
3. 매체 티어와 광고성 배제 기준으로 거른다. 중복 사안은 상위 티어 기사 하나만 남긴다.
4. 자사 브랜드 관련성이 높은 순으로 정리하고, 카테고리별 섹션으로 묶어 HTML 리포트를 생성한다.
5. 각 항목은 다음 형식으로 작성한다:
   - 제목
   - 핵심 요약 (1~2줄)
   - 원문 링크 + **발행일**
   - 시사점 (가능하면 1줄, 자사 관점에서)

## 결과물 규격
- 형식: **카드뉴스 가로 스크롤 레이아웃** HTML 리포트 1개 (카테고리별 섹션, 좌우 스와이프)
- 디자인 템플릿: 루트 `index.html`(직전 회차) 구조를 복제(내용만 교체)
- 저장 위치: **`리포트/` 폴더 안** `YYYY년 N월 M주차 업계 동향 및 주요 이슈.html` + 루트 `index.html`(최신본 사본)
- 주차 계산: 그 주(7일)가 더 많이 걸친 달 기준 + 그 달의 몇 번째 주 (예: 6/29~7/5 → 7월 1주차)
- 카테고리당 카드 6~7개 목표. 각 카드 = **이미지(200px, 카드 상단) 위에 컬러 태그(우상단)+제목(흰 글씨, 최대 3줄)**
  → 흰 본문에 요약 + 시사점 → 하단 구분선 아래 **출처(좌) + 발행일(우, `YYYY.MM.DD`)**
- 카드 이미지는 기사 og:image 를 자동 추출(원본 HTML 파싱; WebFetch 금지). 한글 URL은 percent-encoding.
- 카드 전체가 클릭되어 기사로 이동(stretched-link: `.card` position:relative + `.card-foot a::after`)
- **발행일은 반드시 카드에 표시한다.** 기간 밖 기사가 섞이면 눈으로 바로 걸러지게 하기 위한 장치다.

## 시사점 문체 (AI 말투 금지)
- 사람이 회의에서 말하듯 담백한 평서문. 서술어로 끝낸다. ('~한다', '~해야 한다', '~하는 편이 낫다', '~볼 만하다')
- 금지: 개조식 명사 종결('~선점.', '~배치.', '~검토.'), 컨설팅 버즈워드(USP·포트폴리오·채널 믹스·이원화·서사화·세계관·벤치마킹·로드맵·포지셔닝),
  문장을 잇는 대시(—), '+'·중점으로 단어 욱여넣기, 라벨의 이모지(💡)
- 라벨은 `<b>시사점</b>`. 한 카드에 한두 문장, 40~60자.
- 리포트 상단: 생성일 + 수집 기간 + 핵심 헤드라인 5개(번호 배지). 담당 브랜드 줄은 넣지 않는다.

## 응답 규칙
- 모든 응답과 설명은 항상 한국어로 작성한다.
- PowerShell 터미널에 표시되는 모든 화면(출력, 안내 메시지, 오류 메시지 등)도 한국어로 작성한다.
- Claude가 제공하는 모든 텍스트(설명, 요약, 시사점, UI 문구 등)는 예외 없이 한국어를 사용한다.

## 진행 현황 메모
- 2026-06-29: **6월 4주차(6/22~28) 회차 완성·게시**.
  - 카드뉴스 가로 레이아웃 확정(헤더-본문 정렬, 헤드라인 번호 리스트, 카테고리당 6~7개 카드,
    기사 og:image 썸네일 자동추출, 카드 전체 클릭, noindex).
  - 결과물: `리포트/2026년 6월 4주차 업계 동향 및 주요 이슈.html` + 루트 `index.html`(최신본).
  - **웹 게시 완료**: https://withbrothersjsa.github.io/media-report/ (GitHub Pages, 링크 아는 사람만).
  - 자동화: `리포트생성.bat`(생성+끝에 발행 Y), `발행.bat`(수정 후 발행). 인증=GitHub Desktop(GCM).
- 2026-06-30: **6월 4주차 회차 점검·보완**. 라이브 사이트·기사 링크 27개·썸네일 27개 전부 정상 확인.
  - **섹션별 「이번주 이슈 요약」 박스 신규 추가**(4개 섹션 상단, 섹션 색상 좌측 바 + 배지).
    구체적 수치 포함한 완결형 문단 1개씩. 기존 카드는 그대로 유지.
  - 사용자 피드백 반영: 요약에서 **브랜드 적용·"~할 시점이다" 마무리 문장은 빼고 순수 요약만** 남김.
  - `index.html`·아카이브 리포트 동기화 후 **발행 완료**(커밋 9f0646c). 미커밋 상태였던 발행.bat/ps1도 함께 정리.
  - 신규 양식: 회차 생성 시 카테고리별 카드 + **섹션 상단 '이번주 이슈 요약'(요약만, 브랜드 적용 문구 제외)** 포함.
- 2026-07-06: **7월 1주차(6/29~7/5) 회차 생성 완료**. 카테고리 카드 7/6/6/7개 + 섹션별 '이번주 이슈 요약'(요약만).
  - 핵심 이슈: 틱톡숍 7월 한국 상륙 / 쿠팡 상품명 경쟁사 언급 금지(6.30) / 인코스메틱스 코리아 2026(7.1~3) / 에이피알 2조 클럽·EBD / 무신사 엑스더리그.
  - 결과물: `리포트/2026년 7월 1주차 업계 동향 및 주요 이슈.html` + 루트 `index.html`(최신본 동기화).
  - **검증 완료**: 썸네일 26개·기사 링크 26개 전부 HTTP 200. sedaily만 데스크톱 URL 404 → m.sedaily.com 링크로 교체.
  - og:image는 규칙대로 원본 HTML curl 파싱으로 추출(WebFetch 미사용).
- 2026-07-06: **월요일 오전 자동화 구축(반자동).** 작업 스케줄러 태스크 `업계동향리포트` 등록 —
  매주 **월요일 09:00**, `자동생성.bat`→`자동생성.ps1` 실행. Claude를 **headless**(`claude -p ... --dangerously-skip-permissions`)로
  돌려 지난 7일 동향을 무인 수집·작성하고 index.html까지 갱신(링크 자동 200 점검 포함). **발행(push)은 안 함** → 결과를 브라우저로 자동 열어줌,
  사람이 검토 후 `발행.bat` 더블클릭으로 게시. 로그: `자동생성로그/`.
  - 대화형으로 옆에서 만들려면 기존 `리포트생성.bat` 사용(택1).
  - **완전 자동(생성+발행)으로 바꾸려면**: `자동생성.ps1` 끝에 `git add -A; git commit; git push origin main` 추가(또는 발행 여부 상수화). 현재는 안전상 제외.
  - PC가 켜져 있어야 실행(놓치면 StartWhenAvailable로 다음 부팅 때 따라잡기). 인증=GitHub Desktop(GCM)은 발행 시에만 필요.
- 2026-07-09: **카드 레이아웃 개편 + 시사점 문체 정비 + 수집 규칙 강화.** (참고 디자인: valroad.co.kr/magazine)
  - 레이아웃: 이미지를 156px→**200px로 키우고 제목을 이미지 위 흰 글씨로 올림**(하단 어두운 그라데이션으로 가독성 확보),
    태그는 좌상단→**우상단 사각 배지**, 카드 맨 아래에 **구분선+출처 바(`.card-foot`)** 신설. 요약·시사점은 그대로 유지.
    stretched-link 셀렉터가 `.card-link a::after` → **`.card-foot a::after`** 로 바뀜.
  - 시사점: 개조식 명사 종결·컨설팅 버즈워드·💡 이모지를 걷어내고 **담백한 평서문**으로 26개 전부 재작성. 문체 규칙을 위에 명문화.
  - **7월 1주차 회차의 심각한 결함 발견**: 카드 26개 중 **23개가 수집 기간(6/29~7/5) 밖**이었다.
    이투데이 카드는 2025-06-16(13개월 전), 대한경제는 2026-01-15, '틱톡숍 7월 상륙' 헤드라인의 근거인 서울경제 단독 기사는 **2024-04-16**자였다.
    원인 = 지난 회차 검증이 **링크 HTTP 200만 확인하고 발행일은 확인하지 않았다.** WebSearch는 날짜 필터가 없어 관련성만 높으면 옛 기사를 돌려준다.
  - 대책 3종을 규칙·스크립트에 반영: ①**발행일 전수 검증 필수**(위 '작업 절차' 2번) ②**매체 티어 + 광고성 배제 기준**
    ③**`기사수집.py` 신설** — 검색 대신 매체 기사 목록을 직접 훑어 기간 내 발행분만 뽑는다(발행일 메타태그 파싱).
  - 티어는 **금지 목록이 아니라 채우는 순서**다(사용자 확정). 정론지를 우선하되 부족하면 아래 티어로 내려가고,
    티어와 무관하게 **광고성 기준으로만 버린다**. 못 채우면 기간을 늘리는 대신 카드 수를 줄인다.
  - **리포트 본문은 원복.** 재수집(23장)을 시도했으나 사용자 지시로 되돌리고, **기존 26장 + 새 레이아웃 + 새 시사점 문체**만 남김.
    카드 발행일 표시는 CSS(`.card-date`)만 넣어두고 이번 회차엔 미사용(기존 데이터에 발행일 없음). 다음 회차부터 표시.
  - 반영 범위: `index.html`, `리포트/2026년 7월 1주차 …html`(두 파일 동일), `자동생성.ps1`·`리포트생성.ps1`, `기사수집.py`(신규), 이 CLAUDE.md.
  - **아직 발행(push) 안 함** — 사용자 확인 후 `발행.bat`.
  - `기사수집.py` 한계(다음에 손볼 것): 전자신문·플래텀은 curl 차단, 패션비즈는 JS 렌더링, 한국섬유신문은 page 파라미터 무시.
    어패럴뉴스·이투데이·코스인코리아·코스모닝·바이라인·디지털데일리는 정상 동작. 월요일(직전 7일) 실행엔 충분하다.
- 2026-07-13: **7월 2주차(7/6~7/12) 회차 생성 완료.** 카드 24장 = 뷰티 7 / 패션·잡화 7 / **디바이스 4** / 커머스 공통 6.
  - **자동생성 첫 무인 실행은 실패**했다. 스케줄러 태스크 `업계동향리포트`에 등록된 경로가 옛 폴더명(`…\클로드작업\…`)이라
    현재 폴더(`★★★클로드작업`)를 못 찾았다(LastTaskResult 0x8007010B = 디렉터리 없음). **경로를 수정했고 다음 실행(7/20 09:00)부터 정상.**
    → 이번 회차는 대화형으로 생성. 무인 생성 품질 점검은 7/20으로 미뤄짐.
  - 수집은 규칙대로 `기사수집.py` 우선(144건 확보) → 부족한 디바이스만 웹 검색 보완.
    **PowerShell `>` 리다이렉션은 UTF-16으로 저장돼 TSV가 깨진다. `cmd /c "python … > 파일"` 로 받을 것.**
  - **발행일 전수 검증 통과**: 채택 24건 모두 원문 메타태그 기준 7/6~7/12. 카드에 발행일 표시 시작(`.card-date`).
    웹 검색은 이번에도 옛 기사를 최신처럼 돌려줬다(에이피알 유럽 유통계약 = **2024-07-08**자 / 이투데이 홈뷰티 기사 = 7/4) → 둘 다 배제.
  - **디바이스 섹션은 4장.** 기간 내 디바이스 전문 보도가 적어 규칙대로 기간을 늘리지 않고 카드 수를 줄였다(요약 박스에 그 사실을 명시).
  - 핵심 이슈: 식약처 화장품 안전성 평가 입법예고(8/18까지 의견수렴) / EU PPWR 8.12 시행 / PDRN·시술 표방 과장광고 상반기 41건 적발 /
    CJ온스타일 숏폼 주문 300만개 / 지그재그·무신사·컬리의 화장품 제조·브랜드 소유 진입.
  - 검증: 기사 링크 24개·썸네일 24개 전부 HTTP 200. (byline은 짧은 UA면 403 → 점검 시 정식 UA 사용)
  - 결과물: `리포트/2026년 7월 2주차 업계 동향 및 주요 이슈.html` + 루트 `index.html`(동기화). **아직 발행(push) 안 함** → 검토 후 `발행.bat`.
- 2026-07-20: **7월 3주차(7/13~7/19) 회차 무인 자동 생성 완료.** 카드 25장 = 뷰티 7 / 패션·잡화 7 / **디바이스 4** / 커머스 공통 7.
  - **경로 수정 후 첫 무인 실행이 정상 동작**했다(7/13 메모의 숙제 해소). 수집은 규칙대로 `기사수집.py` 우선.
  - **`cmd /c "python … > 파일"`은 한글 경로에서 깨진다**(cmd 코드페이지). Bash 툴에서 `python … > 파일` 직접 실행이 정답.
    PowerShell `>`(UTF-16) 문제와 별개의 이슈이니 둘 다 피할 것.
  - 1차 수집 126건(코스인34·이투데이27·어패럴20·바이라인16·코스모닝15·한국섬유14) → 디바이스가 비어
    **ad-hoc 보강 스크립트 2벌 신설**(`보강수집.py`=시사저널e·장업신문·뷰티누리·전자신문 / `디바이스수집.py`=히트뉴스·비즈니스플러스·뷰티경제).
    장업신문 39건·시사저널e 19건·뷰티경제 17건 추가 확보. **뷰티누리·전자신문은 curl 차단으로 0건**(기존 한계와 동일).
  - **발행일 전수 검증 통과**: 채택 25건 모두 원문 메타태그 기준 7/13~7/19. 링크 25개·썸네일 25개 전부 HTTP 200.
    **WebSearch는 이번에도 옛 기사를 최신처럼 돌려줬다** — 아이웨어 기사=2025-12-17(7개월 전), 코스모닝 규제=6/28,
    시사저널e 디바이스=5/6, 이투데이 홈케어=7/4. 전부 배제. 검색 결과는 예외 없이 curl 검증 후 채택할 것.
  - **디바이스 섹션은 4장**(2주 연속). 기간 내 디바이스 전문 보도가 거의 없어 기간을 늘리지 않고 수를 줄였고,
    미용 의료기기 시장 데이터(히트뉴스)·에이피알 중간배당(한국섬유신문)·힐로웨이브 4등급 의료기기(장업신문)로 채웠다.
  - **아이웨어 기간 내 보도 0건** → 규칙대로 인접 주제로 확장. 레인부츠 **락피쉬웨더웨어 중국 전략**(리끌로우 직접 경쟁사) 확보.
  - 이미지 팁: ktnews·장업신문은 og:image가 `_v150` 썸네일(15~20KB)이라 흐리다. `/news/photo/…{번호}.jpg`(`_v150` 제거)가
    있으면 고해상도로 교체 가능(147253·147263·99260은 성공, 나머지는 404). byline 한글 이미지 URL은 percent-encoding 필수.
  - 핵심 이슈: 식약처 **AI 생성 가짜 전문가 이미지 광고 금지**·GCORAS 하반기 신설 / EU 소액소포 면세 7/1 전면 폐지 /
    6월 대일본 수출 +19.3%지만 마스크팩 -31.8% / 미용 의료기기 시장 8,091억(2배) / 네이버 AI탭 1000만 / 우버-딜리버리히어로 22조.
  - 결과물: `리포트/2026년 7월 3주차 업계 동향 및 주요 이슈.html` + 루트 `index.html`(동기화). **아직 발행(push) 안 함** → 검토 후 `발행.bat`.
- **다음 회차**: 7/27(월) 09:00 자동 생성 → '7월 4주차' 초안. 검토 후 `발행.bat`. (수동 원하면 `리포트생성.bat`)
- **다음 할 일(선택)**: `보강수집.py`·`디바이스수집.py`의 매체를 **`기사수집.py`에 정식 병합**(장업신문·시사저널e·히트뉴스·뷰티경제는 수확이 좋았다).
  **아이웨어 전용 소스 확보**(한국안경신문·안경원라이프 등 — 2주 연속 기간 내 0건). 뷰티누리·전자신문 curl 차단 우회.
  과거 06-24 회차 파일명 통일(보류 중).
